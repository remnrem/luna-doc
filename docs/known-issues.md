# Known issues

- although we should be able to read gzipped-compressed plain text
  files as inputs, Luna will truncate the last few lines currently:
  need to fix

- check/document that [`SPINDLES`](ref/spindles-so.md#spindles) command can use `fwhm` parameterization

- empirical threshold setting for [`SPINDLES`](ref/spindles-so.md#spindles) appears broken in v0.24

- documentation: need to document new [`FILTER`](ref/fir-filters.md#filter) features, e.g. taking a `file`, etc



